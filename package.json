{
  "name": "iamedu-front",
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./static/main.css --minify && npx elm-land build"
  },
  "devDependencies": {
    "tailwindcss": "^3.2.7"
  }
}
