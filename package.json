{
  "name": "iamedu-front",
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./static/main.css --minify && npx elm-land build",
    "watch": "npx tailwindcss -i ./src/input.css -o ./static/main.css --watch & elm-land server"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.3",
    "@tailwindcss/typography": "^0.5.9",
    "tailwindcss": "^3.2.7"
  }
}
